<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <AssemblyName>MairsTrivia</AssemblyName>
  </PropertyGroup>
  <!-- Add Dalamud package references through the supported local development feed in implementation stage. -->
</Project>
