namespace MairsTrivia.Editor;

/// <summary>Application composition root. UI technology is intentionally selected in a later stage.</summary>
internal static class Program
{
    [STAThread]
    private static void Main() => Console.WriteLine("Mair's Trivia Question Set Editor foundation");
}
